import _ from 'lodash';import React, { Component } from 'react';import { connect } from 'react-redux';import { State } from '../reducers';import HUD from './HUD';import TextLayer from './TextLayer';import CanvasScreen from './CanvasScreen';import { CanvasBattleField } from './CanvasBattleField';// Convert to class component to support React 15.3.2 (no Hooks)class BattleFieldScene extends Component<State> {  canvasRef: { current: HTMLCanvasElement | null } = { current: null };  render() {    const { props } = this;    return (      <div>        <CanvasScreen refFn={canvas => this.canvasRef.current = canvas} />        <HUD />        <TextLayer texts={props.texts} />        <CanvasBattleField gameState={props} canvasRef={this.canvasRef} />      </div>    );  }}export default connect<State>(_.identity)(BattleFieldScene);
